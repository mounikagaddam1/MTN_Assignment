import {UserAdd} from './users.model';

// describe('Store selector', () => {
//     it('SHOULD create a model', () => {
//       let addUser :UserAdd
//       expect(typeof(Object.keys(addUser))).toBe('string');
//     });
//   })
